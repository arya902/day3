#include<stdio.h>
int main()
{
    int a,b,result;
    printf("enter the integers");
    scanf("%d%d",&a,&b);
    sum(a,b);
    return 0;
}
int sum(int a,int b)
{
    int result;
    result =a+b;
    printf("sum is %d",result);
    return result;
}
