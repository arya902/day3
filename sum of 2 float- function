#include<stdio.h>
int sum(float a,float b);
int main()
{
    float a,b,result;
    printf("enter the integers");
    scanf("%f%f",&a,&b);
    sum(a,b);
    return 0;
}
int sum(float a,float b)
{
    float result;
    result =a+b;
    printf("sum is %f",result);
    return result;
}
